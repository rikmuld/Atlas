module CityScreen {

    export const NAME = "CityScreen"

    export class CityScreen implements GuiManager.IScreen {
        constructor() {

        }

        static setup() {
            
        }

        update(delta: number) {
           
        }

        render(delta: number) {
            
        }
    }
}